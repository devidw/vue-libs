{
  "name": "vue-libs",
  "version": "0.1.1",
  "scripts": {
    "release": "bumpp package.json packages/*/package.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "bumpp": "^8.2.1"
  }
}
